.jumbotron.mixture-hero
  .container
    %h1 Important items
    %p Mixture の大事なものを管理しよう。

.container
  .row
    = link_to new_important_item_path do
      = button_tag '新規作成', class: 'btn btn-default btn-lg'

    %table.table.table-hover.list
      %thead
        %tr
          %th id
          %th name
          %th experience
          %th activity
          %th
      %tbody
      - @important_items.each do |item|
        %tr
          %td= item.id
          %td= item.name
          %td= item.experience
          %td= item.activity
          %td
            = link_to edit_important_item_path item do
              = button_tag 'Edit', class: 'btn btn-default'

    = paginate @important_items
